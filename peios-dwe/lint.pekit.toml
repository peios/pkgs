[allow]
"package.name.style" = "peios-dwe is an intentionally private DWE image fixture and is forbidden from public repository publication"
"source.reproducible" = "peios-dwe is a private local image fixture whose security policy forbids public repository publication"
