[allow]
"source.discovery" = "This private binary seed is pinned to the exact Go 1.24.6 bootstrap required by the source-built compiler, not an independently tracked toolchain"
"source.versions.ceiling" = "The exact Go 1.24.6 binary seed is a deliberate bootstrap root; moving it is coordinated with the source compiler requirement"
"source.signature.required" = "go.dev publishes release hashes over HTTPS but no detached source signatures; the immutable Pekit lock pins the accepted bootstrap bytes"
"build.test" = "The seed has no independent product test surface; build.main verifies its compiler, formatter, target, and compiler-source installation before the source-built Go consumer uses it"
