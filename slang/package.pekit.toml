# Shared defaults layer for the slang members (a bare package.pekit.toml
# is never a package once named members exist).

format = "peipkg"

[package]
version      = "{{version}}-1"
architecture = "x86_64"
license      = "GPL-2.0-or-later"
homepage     = "https://www.jedsoft.org/slang/"
