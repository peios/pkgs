# Peios llvm patch series — applied in order by pekit during source
# materialisation ([source].patches in pekit.toml).
gcc16/missing-cstdint.patch  # GCC 16's libstdc++ dropped transitive <cstdint>; SmallVector.h uses uint64_t bare (upstream-fixed after 18)
