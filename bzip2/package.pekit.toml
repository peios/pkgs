# Shared defaults layer for the bzip2 members (a bare package.pekit.toml is
# never a package once named members exist).

format = "peipkg"

[package]
version      = "{{version}}-1"
architecture = "x86_64"
license      = "bzip2-1.0.6"
homepage     = "https://sourceware.org/bzip2/"
