# libpci — the PCI access runtime library.

[package]
name        = "libpci"
description = "pciutils - libpci PCI access runtime library"

[dependencies]
glibc = ">= 2.43"

[files]
":usr/lib/x86_64-linux-peios/libpci.so.3*" = "usr/lib/x86_64-linux-peios"
