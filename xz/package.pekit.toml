# Shared defaults layer for the xz members (a bare package.pekit.toml is
# never a package once named members exist).

format = "peipkg"

[package]
version      = "{{version}}-1"
architecture = "x86_64"
homepage     = "https://tukaani.org/xz/"
license      = "0BSD"
