# Shared defaults layer for the flex members (a bare package.pekit.toml is
# never a package once named members exist).

format = "peipkg"

[package]
version      = "{{version}}-1"
architecture = "x86_64"
license      = "BSD-2-Clause"
homepage     = "https://github.com/westes/flex"
