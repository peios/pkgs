# libconfig — the configuration-parsing runtime library.

[package]
name        = "libconfig"
description = "libconfig - structured configuration file runtime library"

[dependencies]
glibc = ">= 2.43"

[files]
"@source:COPYING.LIB" = "usr/share/licenses/libconfig/"
":usr/lib/x86_64-linux-peios/libconfig.so.1*" = "usr/lib/x86_64-linux-peios"
