# glibc-langpack-<lang> — compiled, per-language locale data (arch-specific).
#
# One package is generated per language present in the "locale" build output:
# [multipack.enum.files] lists the per-locale dirs, captures the language code
# (regex group 1), dedupes, and emits a package per unique code.
# [package].name templates {{multipack}} to produce glibc-langpack-de,
# glibc-langpack-fr, etc.; {{multipack}} holds the code in the fields below.
#
# Inherits base metadata, including architecture = x86_64: compiled locale data
# is native-endian binary, so NOT noarch. The locale SOURCE definitions and the
# message catalogs live in the noarch glibc-locale-source.

[multipack.enum.files]
path = "locale:usr/lib/x86_64-linux-peios/locale/*"
regex = '^([a-z]+)_'

[package]
name = "org.gnu.glibc-langpack-{{multipack}}"
description = "GNU C Library - {{multipack}} locale data"

[provides]
"glibc-langpack-{{multipack}}" = "{{version}}-5"

[replaces]
"glibc-langpack-{{multipack}}" = "<= {{version}}-4"

[dependencies]
# Compiled locale data is loaded by glibc at runtime and its binary format is
# tied to the glibc that produced it, so pin the matching family revision.
"org.gnu.glibc" = "{{version}}-7"

[files]
"@source:COPYING*" = "usr/share/licenses/org.gnu.glibc-langpack-{{multipack}}/"
"@source:LICENSES" = "usr/share/licenses/org.gnu.glibc-langpack-{{multipack}}/"
# Every locale for this language (de_DE, de_DE.utf8, de_AT, ...) and its LC_*
# files, taken from the locale build.
"locale:usr/lib/x86_64-linux-peios/locale/{{multipack}}_*/**" = "usr/lib/x86_64-linux-peios/locale/"
