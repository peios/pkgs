[allow]
"source.signature.required" = "GNOME publishes libxml2 release archives and checksum sidecars but no detached maintainer signatures; HTTPS plus Pekit's immutable SHA-256 lock, checked against the sidecar on acceptance, is the available provenance boundary"
