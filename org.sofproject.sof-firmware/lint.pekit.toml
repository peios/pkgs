[allow]
"source.signature.required" = "The official sof-bin release channel publishes HTTPS assets but no detached signatures; Pekit's immutable lock pins accepted bytes and every staged firmware blob is independently PIP-signed"
