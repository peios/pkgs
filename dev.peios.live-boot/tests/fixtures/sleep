#!/usr/bin/sh
count=0
if [ -r "$PEIOS_LIVE_BOOT_TEST_LOG/sleep.count" ]; then
    count=$(cat "$PEIOS_LIVE_BOOT_TEST_LOG/sleep.count")
fi
count=$((count + 1))
printf '%s\n' "$count" > "$PEIOS_LIVE_BOOT_TEST_LOG/sleep.count"
