# zstd — the command-line tools (libzstd is compiled in, not linked).

# PSPU §5.24: a payload carrying man pages SHOULD declare man-db, so that
# `man -k` and `apropos` find them instead of falling back to a filesystem
# scan. Top-level, not inside a table -- TOML would otherwise scope it to
# whichever table precedes it.
side_effects = ["man-db"]

[package]
name        = "zstd"
description = "Zstandard - fast lossless compression (zstd, zstdcat, unzstd)"

[dependencies]
glibc = ">= 2.43"

[files]
"@source:LICENSE"   = "usr/share/licenses/zstd/"
":usr/bin/**"       = "usr/bin"
":usr/share/man/**" = "usr/share/man"
