# GNU Patch — strict unified/context diff application for source builds.

out_dir = "out"

[source.url]
url         = "https://ftp.gnu.org/gnu/patch/patch-{{version}}.tar.xz"
listing_url = "https://ftp.gnu.org/gnu/patch/"
extract     = true
root        = "patch-{{version}}"
file_regex  = 'patch-[0-9]+\.[0-9]+(?:\.[0-9]+)?\.tar\.xz'
# Soft maintenance floor: follow authenticated stable releases automatically.
versions    = ">= 2.8"

[source.url.signature]
url          = "{{source_url}}.sig"
of           = "artifact"
key_files    = ["keys/andreas-gruenbacher.asc"]
fingerprints = ["259B3792B3D6D319212CC4DCD5BF9FEB0313653A"]

[build.main]
command = '''
set -eu
SRC="$(pwd)"
cd "$PEKIT_OUT"
ROOT=$(dirname "$(dirname "$PEKIT_OUT")")
CFLAGS="$CFLAGS -ffile-prefix-map=$ROOT=/usr/src/debug/org.gnu.patch -fdebug-prefix-map=$ROOT=/usr/src/debug/org.gnu.patch -fmacro-prefix-map=$ROOT=/usr/src/debug/org.gnu.patch" \
LDFLAGS="$LDFLAGS -Wl,-z,ibt,-z,shstk" \
"$SRC/configure" \
  --build=x86_64-pc-linux-gnu \
  --host=x86_64-pc-linux-gnu \
  --prefix=/usr \
  --bindir=/usr/bin \
  --mandir=/usr/share/man \
  --localedir=/usr/share/locale \
  --disable-silent-rules
make -j"$(nproc)"
make install DESTDIR="$PEKIT_OUT"
rm -rf "$PEKIT_OUT/usr/share/info" "$PEKIT_OUT/usr/share/doc"
gzip -9n "$PEKIT_OUT/usr/share/man/man1/patch.1"

cd "$PEKIT_OUT"
BIN=usr/bin/patch
patchelf --remove-rpath "$BIN" 2>/dev/null || true
debugedit -b "$ROOT" -d /usr/src/debug/org.gnu.patch \
  -l "$PEKIT_OUT/.srclist" -n "$BIN"
bid=$(readelf -n "$BIN" | sed -n 's/^[[:space:]]*Build ID: //p' | head -1)
test -n "$bid"
p2=$(printf %s "$bid" | cut -c1-2)
rest=$(printf %s "$bid" | cut -c3-)
mkdir -p "usr/lib/debug/.build-id/$p2"
objcopy --only-keep-debug "$BIN" "usr/lib/debug/.build-id/$p2/$rest.debug"
chmod 0644 "usr/lib/debug/.build-id/$p2/$rest.debug"
strip "$BIN"
tr '\0' '\n' < "$PEKIT_OUT/.srclist" | LC_ALL=C sort -u \
  | while IFS= read -r rel; do
      test -n "$rel" || continue
      case "$rel" in */) continue;; esac
      test -f "$ROOT/$rel" || continue
      mkdir -p "usr/src/debug/org.gnu.patch/$(dirname "$rel")"
      cp "$ROOT/$rel" "usr/src/debug/org.gnu.patch/$rel"
    done
rm -f "$PEKIT_OUT/.srclist"
'''

[build.main.dependencies.peipkg]
"dev.peios.build-essentials-c" = "*"
"org.sourceware.debugedit"     = "*"
"org.gnu.gettext"              = "*"
"org.gnu.gzip"                 = "*"
"org.nixos.patchelf"           = "*"
"org.perl.perl"                = "*"

[build.main.dependencies.apt]
build-essential = "*"
debugedit       = "*"
gettext         = "*"
gzip            = "*"
patchelf        = "*"
perl            = "*"

[test]
needs = ["main"]
gate  = true
command = '''
set -eu
STAGE="$PEKIT_MAIN_OUT"
BIN="$STAGE/usr/bin/patch"
SMOKE="$PEKIT_OUT/smoke"
cd "$STAGE"

# Upstream's complete regression suite covers normal/context/unified diffs,
# rejects, timestamps, backups, path traversal protections and malformed input.
make check

mkdir -p "$SMOKE/tree"
printf '%s\n' alpha beta gamma > "$SMOKE/tree/input"
printf '%s\n' alpha changed gamma > "$SMOKE/expected"
diff -u --label input --label input \
  "$SMOKE/tree/input" "$SMOKE/expected" > "$SMOKE/change.patch" || :
"$BIN" --directory "$SMOKE/tree" --strip=0 --batch --forward \
  --fuzz=0 --input "$SMOKE/change.patch"
cmp "$SMOKE/tree/input" "$SMOKE/expected"
if "$BIN" --directory "$SMOKE/tree" --strip=0 --batch --forward \
  --fuzz=0 --input "$SMOKE/change.patch"; then
  exit 1
fi

"$BIN" --version | head -1 | grep -F "$PEKIT_VERSION"
readelf -h "$BIN" | grep -Eq 'Type:[[:space:]]+DYN'
readelf -W -l "$BIN" | grep 'GNU_STACK' | grep -F 'RW '
readelf -W -l "$BIN" | grep -F 'GNU_RELRO'
readelf -d "$BIN" | grep -F 'BIND_NOW'
readelf -d "$BIN" | grep -F 'RELR'
readelf -n "$BIN" | grep -E 'x86 feature:.*IBT.*SHSTK'
if readelf -d "$BIN" | grep -Eq '(RPATH|RUNPATH)'; then exit 1; fi
test "$(find "$STAGE/usr/lib/debug/.build-id" -type f -name '*.debug' | wc -l)" -eq 1
test -n "$(find "$STAGE/usr/src/debug/org.gnu.patch" -type f -print -quit)"
'''

[test.dependencies.peipkg]
"dev.peios.build-essentials-c" = "*"
"org.git.kernel.dash" = "*"
"dev.peios.peiosutils" = "*"
"org.gnu.diffutils"    = "*"
"org.gnu.findutils"    = "*"
"org.gnu.grep"         = "*"
"org.gnu.gawk"         = "*"
"org.gnu.sed"          = "*"
"org.gnu.make"         = "*"
"org.gnu.binutils"     = "*"

[test.dependencies.apt]
build-essential = "*"
binutils  = "*"
coreutils = "*"
diffutils = "*"
findutils = "*"
gawk      = "*"
grep      = "*"
make      = "*"
sed       = "*"
