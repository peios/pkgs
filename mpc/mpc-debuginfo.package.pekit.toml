# mpc-debuginfo — separated debug symbols (build-id-indexed).

[package]
description = "GNU MPC - debugging symbols"

[dependencies]
mpc = "{{version}}"

[files]
":usr/lib/debug/**" = "usr/lib/debug"
