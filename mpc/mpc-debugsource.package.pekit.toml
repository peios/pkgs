# mpc-debugsource — mpc sources referenced by the debug info, under
# /usr/src/debug/mpc. Pairs with mpc-debuginfo.

[package]
description = "GNU MPC - debug sources"

[dependencies]
mpc-debuginfo = "{{version}}"

[files]
":usr/src/debug/**" = "usr/src/debug"
