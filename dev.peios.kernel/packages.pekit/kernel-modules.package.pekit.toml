# Catalogue revision overlay: keep all same-family relations aligned with -3.
[package]
name = "dev.peios.kernel-modules"

[dependencies]
"dev.peios.kernel" = "= {{version}}-3"

[provides]
"kernel-modules" = "{{version}}-3"
