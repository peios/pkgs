# Shared defaults layer for the ncurses members (a bare package.pekit.toml
# is never a package once named members exist).

format = "peipkg"

[package]
version      = "{{version}}-1"
architecture = "x86_64"
license      = "MIT"
homepage     = "https://invisible-island.net/ncurses/"
