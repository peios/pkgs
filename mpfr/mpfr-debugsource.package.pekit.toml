# mpfr-debugsource — mpfr sources referenced by the debug info, under
# /usr/src/debug/mpfr. Pairs with mpfr-debuginfo.

[package]
description = "GNU MPFR - debug sources"

[dependencies]
mpfr-debuginfo = "{{version}}"

[files]
":usr/src/debug/**" = "usr/src/debug"
