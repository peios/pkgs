# mpfr-debuginfo — separated debug symbols (build-id-indexed).

[package]
description = "GNU MPFR - debugging symbols"

[dependencies]
mpfr = "{{version}}"

[files]
":usr/lib/debug/**" = "usr/lib/debug"
