# Shared defaults layer for the capstone members (a bare package.pekit.toml
# is never a package once named members exist).

format = "peipkg"

[package]
version      = "{{version}}-1"
architecture = "x86_64"
license      = "BSD-3-Clause"
homepage     = "https://www.capstone-engine.org/"
