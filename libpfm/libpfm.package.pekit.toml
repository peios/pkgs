# libpfm — the performance event encoding runtime library.

[package]
name        = "libpfm"
description = "libpfm - hardware performance event encoding runtime library"

[dependencies]
glibc = ">= 2.43"

[files]
"@source:COPYING" = "usr/share/licenses/libpfm/"
":usr/lib/x86_64-linux-peios/libpfm.so.4*" = "usr/lib/x86_64-linux-peios"
