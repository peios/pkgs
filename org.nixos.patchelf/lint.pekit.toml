[allow]
"source.signature.required" = "Upstream publishes HTTPS release archives and GitHub digests but no maintainer-authenticated detached signatures; the immutable lock pins accepted bytes"
"build.test" = "The upstream suite and staged interpreter, run-path, dependency, and page-size probes run inside build.main so package and publish fail closed"
