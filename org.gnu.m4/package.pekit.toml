# Metadata shared by the GNU M4 package family. The installed program is
# GPL-3.0-or-later; the broader expression also covers the bundled Gnulib,
# build helpers and manual retained by the corresponding source package.

format = "peipkg"

[package]
version       = "{{version}}-1"
architecture  = "x86_64"
license       = "GPL-3.0-or-later AND GPL-2.0-or-later AND LGPL-2.1-or-later AND GFDL-1.3-or-later AND MIT AND FSFULLR"
license_class = "free"
homepage      = "https://www.gnu.org/software/m4/"
